import 'package:flutter/material.dart';

class SubtagChipWeb extends StatelessWidget {
  const SubtagChipWeb({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}